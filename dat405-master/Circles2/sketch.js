//let size= random(500);
let size;
let xco = (random(500));
let yco = (random(500));
function setup() {
  createCanvas(500,500);
  frameRate(30);
  noStroke();
}

function draw() {

  size= random(100);
  customShape ();

}
function customShape(){
  fill (random(255);
  stroke(0,0,0);
  ellipse (xco, yco ,size,size 0);
  fill (random(0);
  ellipse (xco, yco, size - 10, size - 10);
  fill (random(255),random(255),random(255),random(50));
  ellipse (xco, yco, size - 20, size - 20);
  fill (random(255),random(255),random(255),random(50));
  ellipse (xco, yco, size - 30, size - 30);
  fill (random(255),random(255),random(255),random(50));
  ellipse (xco, yco, size - 40, size - 40);
  fill (random(255),random(255),random(255),random(50));
  ellipse (xco, yco, size - 50, size - 50);
}

123
